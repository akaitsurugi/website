{% extends 'libraryItemBook.html.twig' %}
{# The only thing this is currently used for is to separate it from its parent #}
{# so that it'll show up in the library page. #}
{# Eventually, we can change this so books have epub, web, etc., like books #}
